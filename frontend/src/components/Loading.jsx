import React from 'react';
const Loading = () => (
    <div className="flex flex-col items-center w-full h-screen">
        <h2 className="MainHeading">Loading...</h2>
    </div>
);

export default Loading;
